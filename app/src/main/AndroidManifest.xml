<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.upc.jonatan.eetakemongo">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartAct" android:screenOrientation="locked" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuAct" android:screenOrientation="locked" />
        <activity android:name=".Register" android:screenOrientation="locked" />
        <activity android:name=".MainActivity"  android:screenOrientation="locked"  />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".CazarAct" android:screenOrientation="locked"
            android:label="@string/title_activity_cazar" />
        <activity android:name=".EtakedexActivity" android:screenOrientation="locked"  />
        <activity android:name=".EetakedexAdapter" />
        <activity android:name=".EtakemonInfoAct" android:screenOrientation="locked" />
        <activity android:name=".RankingAct" android:screenOrientation="locked" />
        <activity android:name=".RankingAdapter" />
        <activity android:name=".PerfilAct" android:screenOrientation="locked"  />
        <activity android:name=".ActualizarAct" android:screenOrientation="locked" />
        <activity android:name=".WikilistAct" android:screenOrientation="locked" />
        <activity android:name=".WikilistAdapter" />
        <activity android:name=".WikilistInfoAct" android:screenOrientation="locked" ></activity>
    </application>

</manifest>